"""
hackingTesterScript.py
Marcus Viscardi     July 28, 2021

Just a file to test small bits of code, with a little bit more
    permanence than coding in the console.


I'll probably comment stuff out to silence pycharm's duplicate code warning
"""


# def find_newest_matching_file(path_str):
#     # A tool that I'll want to use to grab the most recent file
#     from glob import glob
#     import os
#     list_of_files = glob(path_str)
#     try:
#         latest_file = max(list_of_files, key=os.path.getctime)
#         return latest_file
#     except ValueError:
#         raise ValueError(f"Failed to find any files matching \"{path_str}\"")


if __name__ == '__main__':
    # print(find_newest_matching_file("./*.csv"))
    pass
